name: budz_test
description: A technical test project to Budz.
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  #State Management
  flutter_bloc: ^8.1.3
  #Navigation, Routing and Dependency Injection
  flutter_modular: ^6.3.2
  #Http
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
 

flutter:
  uses-material-design: true
